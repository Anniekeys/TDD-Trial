const multiply = (x, y) => {
    return x * y;
};

const divide = (x, y) =>{
    return x / y;
}

const isOdd = (number) => {
    return number % 2 !== 0;
}


module.exports = { multiply, divide, isOdd };